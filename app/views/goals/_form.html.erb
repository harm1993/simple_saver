<%= form_for(goal) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :amount %>
  <%= f.text_field :amount %>
  <%= f.label :deadline %>
  <%= f.date_select :deadline %>
  <%= f.submit class: "s-button"%>
<% end %>
