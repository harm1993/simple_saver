<div class='allgoals'>
<h1>All goals</h1>

<%= link_to "Create new goal", new_goal_path, class: "s-button" %>

<hr>


<h3>Current goals</h3>
<ul>
  <% if @current_goal %>
    <%= link_to(goal_path(@current_goal)) do %>
      <li>
        <%= @current_goal.name %>
    <% end %>
        <%= @current_goal.saved %> | <%= @current_goal.amount %>
      </li>
  <% end %>
</ul>

<h3>Completed goals</h3>
<ul>
  <% @completed_goals.each do |goal| %>

    <li>
      <%= link_to(goal_path(goal)) do %>
        <%= goal.name %>
      <% end %>
        | <%= goal.amount %>
    </li>
  <% end %>
</ul>
</div>
