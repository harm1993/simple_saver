<% categories.each do |category| %>
  <h3><%= link_to category.name, category %></h3>
  <p><%= category.tot_month %> / <%= category.goal_per_month %></p>

  <div class="state-bar w-50" data-type="line" data-percentage="<%= category.spent_percentage %>"></div>
<% end %>
