<div class="container mt-4 d-flex justify-content-around"> 
  <div class="main">
    <h1> <%= @category.name %> overwiev </h1>
    <p class="mt-4">Hi <%= current_user.first_name %>!
    <br><br>
    This month, you've spent <%= @category.tot_month %> of the <%= @category.goal_per_month %>€.
    <br>
    <% if @category.spent_percentage < 0.50 %>
      Not bad!<br>
      Keep up the good work and your ---goalname will thank you.
    <% else %>
      Keep an eye on it, your ---goalname needs you!
    <% end %></p>
    
    <ul class="list-unstyled text-left">
      <li class="p-2"><%= link_to ">Update category", edit_category_path(@category) %></li>
      <li class="p-2"><%= link_to ">Delete category", category_path(@category), method: :delete %></li>  
      <li class="p-2"><%= link_to ">Add an expense",  new_category_expense_path(@category) %></li>
    </ul>
  </div>
  
  <div class="text-center">
    <h1 class="text-left"><%= Date::MONTHNAMES[Date.today.month] %></h1>
    <%= render '/expenses/index' %>
  </div>
</div>

<div id="Rectangle">
  <div id="SpeechBubble" data-percentage="<%= @category.spent_percentage %>">
  <% if @category.spent_percentage * 100 < 40 %>
    Kudos! You're doing great!
  <% elsif @category.spent_percentage * 100 > 40 && @category.spent_percentage * 100< 70 %>
    Slow down, buddy!
  <% elsif @category.spent_percentage * 100 > 70 %>
    Watch out, kiddo!
  <% else %>
    Ahi, ahi!
  <% end %> 
  You've spent <%= @category.tot_month.to_i %> of the <%= @category.goal_per_month %>€.</div>
</div>